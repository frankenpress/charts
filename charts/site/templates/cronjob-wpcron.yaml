{{- if .Values.wpCron.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ printf "%s-wpcron" (include "common.names.fullname" .) | trunc 52 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
spec:
  schedule: {{ .Values.wpCron.schedule | quote }}
  concurrencyPolicy: {{ .Values.wpCron.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.wpCron.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.wpCron.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{ include "site.serviceAccountName" . }}
          {{- if .Values.image.pullSecrets }}
          imagePullSecrets:
            {{- range .Values.image.pullSecrets }}
            - name: {{ . }}
            {{- end }}
          {{- end }}
          securityContext: {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: wpcron
              image: {{ include "site.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              securityContext: {{- toYaml .Values.containerSecurityContext | nindent 16 }}
              command:
                - /bin/sh
                - -c
                - |
                  set -eu
                  WP="wp --allow-root --path=/app/web/wp"
                  # Skip cycles that fire before `wp core install` has
                  # populated wp_options (post-install Helm hook hasn't
                  # run yet, or operator has paused the deployment).
                  # Erroring out on every fire would accumulate
                  # restarts + clutter logs without doing useful work.
                  if ! $WP core is-installed >/dev/null 2>&1; then
                    echo "[wpcron] WP not yet installed; skipping this cycle"
                    exit 0
                  fi
                  exec $WP cron event run --due-now
              envFrom:
                - configMapRef:
                    name: {{ printf "%s-env" (include "common.names.fullname" .) }}
                {{- if .Values.extraEnvVarsCM }}
                - configMapRef:
                    name: {{ tpl .Values.extraEnvVarsCM . }}
                {{- end }}
                {{- if .Values.extraEnvVarsSecret }}
                - secretRef:
                    name: {{ tpl .Values.extraEnvVarsSecret . }}
                {{- end }}
              env:
                - name: DB_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "site.databaseSecretName" . }}
                      key: {{ include "site.databaseSecretPasswordKey" . }}
                - name: FP_S3_KEY
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "site.s3SecretName" . }}
                      key: {{ include "site.s3AccessKeyKey" . }}
                - name: FP_S3_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "site.s3SecretName" . }}
                      key: {{ include "site.s3SecretKeyKey" . }}
                {{- range $name := list "AUTH_KEY" "SECURE_AUTH_KEY" "LOGGED_IN_KEY" "NONCE_KEY" "AUTH_SALT" "SECURE_AUTH_SALT" "LOGGED_IN_SALT" "NONCE_SALT" }}
                - name: {{ $name }}
                  valueFrom:
                    secretKeyRef:
                      name: {{ include "site.secretName" $ }}
                      key: {{ $name }}
                {{- end }}
                {{- include "site.smtpEnvFromSecret" . | nindent 16 }}
              resources: {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
                - name: data
                  mountPath: /data
                - name: config
                  mountPath: /config
          volumes:
            - name: tmp
              emptyDir:
                medium: Memory
                sizeLimit: {{ .Values.tmpfsSize.tmp }}
            - name: data
              emptyDir:
                medium: Memory
                sizeLimit: {{ .Values.tmpfsSize.data }}
            - name: config
              emptyDir:
                medium: Memory
                sizeLimit: {{ .Values.tmpfsSize.config }}
{{- end }}
